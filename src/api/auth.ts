import type { SigninForm, signupForm } from '@/interface/auth'
import { AxiosInstance } from "@/plugins/axios";

export function signin(data: SigninForm) {
  
}

export function signup(data: signupForm) {

}